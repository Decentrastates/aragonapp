import { drawPlugin } from './drawPlugin';

/**
 * The subdomain used for the draw plugin
 */
export const DRAW_PLUGIN_SUBDOMAIN = drawPlugin.subdomain;

/**
 * The plugin ID used for the draw plugin
 */
export const DRAW_PLUGIN_ID = 'draw';